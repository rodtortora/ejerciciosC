#include <stdio.h>
#include <stdlib.h>

int main()
{

}

// Recorrer arbol, mostrar todos los nodos in order:

public void mostrar(Arbol A) {
    if (A != NULL) {
        printf("%d",A->dato);
        mostrar(Arbol A->izq);
    }
    else


}
